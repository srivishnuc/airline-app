import React from "react";

const PassengerCheckInDetails = ({ flight, name, isCheckedIn, seatno, services }) => {
    console.log({ flight, name, isCheckedIn, seatno, services })
    return (
        <></>
    )

}

export default PassengerCheckInDetails